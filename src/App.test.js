import React from 'react';
import { render, waitForElement } from '@testing-library/react';
// import App from './App';
import Login from './pages/DevForm'

describe('renders learn react link', () => {
  it('should handleLogin when form has been submited', () => {
    // const { getByTestId } = render(<Login/>)
    // const fielNode = await waitForElement(() => {})
    // renderizar o componente
    // buscar o input
    // digitar no input
    // buscar o botao
    // clicar no botao
    // fazer login
    // verificar se o mapa foi renderizado
    // verificar se os repositorios foram renderizados
  })
});
